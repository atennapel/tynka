import "lib/Base" (...)
import "lib/JVM" (...)
import "lib/IO" as IO

def return = IO/return
def (>>=) = IO/bind
def (++) = appendStr
def (==) = equalsStr

def main (_ : Array Str) : IO Unit :=
  IO/fixIO \go. do
    name <- getLn;
    if name == "exit" then
      return U
    else do
      putLn ("hi " ++ name);
      putLn (lengthStr name);
      go
